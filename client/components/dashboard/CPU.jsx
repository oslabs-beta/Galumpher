import React from 'react';
import {Bar} from 'react-chartjs-2';

const CPU = () => {

  return (
    <div className="cpu">
      <h5>CPU Usage</h5>
      <Bar data={} options={} />
    </div>
  );
};


export default CPU;